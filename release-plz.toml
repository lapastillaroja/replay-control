[workspace]
# Don't publish to crates.io — this is a private application
publish = false
# Create git tags and GitHub Releases
git_release_enable = true
git_tag_enable = true
# Single version tag for the workspace (v0.1.0, not crate-v0.1.0)
git_tag_name = "v{{ version }}"
