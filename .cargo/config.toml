[build]
# Limit parallelism to avoid freezing the machine during compilation.
# Uses half the available cores (8 of 16), leaving the rest for the OS/browser.
jobs = 8

[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
